## Backend Pack

- Start with `backend-core` when the problem is architectural, operational, or spans multiple backend concerns.
- Default to feature-first packages and one-way dependencies.
- Use `backend-go-project-layout` before inventing technical-layer folders.
- Route into `backend-go-*` only after the package boundary is clear.
