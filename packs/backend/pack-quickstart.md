## Backend Pack

- Start with `backend-core` when the problem is architectural, operational, or spans multiple backend concerns.
- Route into `backend-go-*` when the task becomes Go-specific.
- Keep shared backend conventions language-agnostic; keep implementation details in language or framework skills.
