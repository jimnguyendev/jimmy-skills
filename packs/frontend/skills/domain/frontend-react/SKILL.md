---
name: frontend-react
description: "React frontend implementation guidance for component boundaries, state flow, rendering strategy, server/client data loading, and testable UI structure. Use when the codebase is React or when the user explicitly wants React patterns on top of shared frontend conventions."
user-invocable: false
license: MIT
compatibility: Designed for Claude Code or similar AI coding agents.
metadata:
  author: jimnguyendev
  version: "1.0.0"
allowed-tools: Read Edit Write Glob Grep Bash(git:*) Agent AskUserQuestion
---

# Frontend React

This is the React entry point for the frontend pack.

## Use It For

1. Component composition and state ownership
2. Data fetching integration with backend APIs
3. Suspense, transitions, and rendering boundaries
4. React testing strategy and contract-safe UI changes

Pair this with `jimmy-skills@frontend-core` for shared frontend concerns.
